services:
  - type: web
    name: AtmosPro
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn servidor_meteo:app"
    envVars:
      - key: FLASK_ENV
        value: production
